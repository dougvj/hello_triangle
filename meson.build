project('hello_triangle', 'c')

sources=[
  'main.c'
  ]

dependencies = [
  dependency('gl'),
  dependency('sdl2'),
  dependency('glew'),
  dependency('glu'),
]

executable('hello_triangle', sources, dependencies: dependencies)
